version: '3.8'

services:
  spring-boot-test:
    image: registry.cn-hangzhou.aliyuncs.com/sunkolin/spring-boot-test:latest
    container_name: spring-boot-test
    restart: unless-stopped
    ports:
      - "6005:8080"
    volumes:
      - /tmp/app:/app
    environment:
      - APP_NAME=spring-boot-test
      - APP_VERSION=1.0.0
      - APP_PORT=6005
      - APP_ENV=prod